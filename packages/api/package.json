{
  "name": "@swipeme.io/api",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist ./tsconfig.build.tsbuildinfo",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@swipeme.io/common": "1.0.0"
  },
  "devDependencies": {
  }
}
