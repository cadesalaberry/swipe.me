{
  "name": "@swipeme.io/common",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist ./tsconfig.build.tsbuildinfo",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
  },
  "devDependencies": {}
}
